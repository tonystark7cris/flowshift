name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - name: Install ruff
      run: pip install ruff
    - name: Check formatting
      run: ruff format --check src/ tests/
    - name: Check linting rules
      run: ruff check src/ tests/

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - name: Install tools
      run: pip install pip-audit bandit
    - name: Audit dependencies for known vulnerabilities
      run: pip install -e ".[dev]" && pip-audit
    - name: Static security analysis
      run: bandit -r src/flowshift/ -ll --skip B101

  test:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install package with dev dependencies
      run: python -m pip install -e ".[dev]"
    - name: Run test suite with coverage
      run: |
        pytest tests/ -v --cov=flowshift --cov-report=term-missing --cov-report=xml \
          -x --tb=short -q
    - name: Upload coverage
      if: matrix.python-version == '3.12'
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: coverage.xml

  spark-test:
    name: Spark Integration Tests
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - name: Install package with Spark dependencies
      run: python -m pip install setuptools && python -m pip install -e ".[dev,spark]"
    - name: Run Spark integration tests
      run: |
        pytest tests/test_spark_engine.py -v --tb=short -q
